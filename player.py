class Player:
    def __init__(self, cards, points_sum, status, name, addr):
        self.cards = cards
        self.points_sum = points_sum
        self.status = status
        self.name = name
        self.addr = addr